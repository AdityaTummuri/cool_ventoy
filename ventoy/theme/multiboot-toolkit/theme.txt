# =============================================================================
# MULTIBOOT TOOLKIT — GRUB2 Theme
# =============================================================================
# Ventoy-compatible GRUB2 theme for a professional multiboot USB toolkit.
# Resolution: 1920x1080
#
# References:
#   - https://www.ventoy.net/en/plugin_theme.html
#   - https://www.gnu.org/software/grub/manual/grub/grub.html
#   - http://wiki.rosalab.ru/en/index.php/Grub2_theme_tutorial
# =============================================================================

# --- Global Properties -------------------------------------------------------
title-text: ""
desktop-image: "background.png"
desktop-color: "#1a1a2e"
terminal-font: "Unifont Regular 16"
terminal-left: "0"
terminal-top: "0"
terminal-width: "100%"
terminal-height: "100%"

# --- Boot Menu ---------------------------------------------------------------
# Centered menu area with professional dark styling
+ boot_menu {
    left   = 15%
    top    = 28%
    width  = 70%
    height = 55%

    item_font        = "Unifont Regular 16"
    item_color       = "#cccccc"
    selected_item_color = "#2a9d8f"

    item_height      = 36
    item_spacing     = 4
    item_padding     = 12
    icon_width       = 32
    icon_height      = 32
    item_icon_space  = 16

    selected_item_pixmap_style = "select_*.png"

    scrollbar              = true
    scrollbar_width        = 8
    scrollbar_thumb        = "scrollbar_thumb.png"
    scrollbar_frame        = "scrollbar_frame.png"
    scrollbar_slice        = "center"

    menu_pixmap_style = "menu_*.png"
}

# --- Hotkey Tips (bottom left) -----------------------------------------------
# Displays F1-F5 hotkey hints — uses Ventoy's built-in variable
+ hbox {
    left   = 3%
    top    = 95%-30
    width  = 50%
    height = 25

    + label {
        text  = "@VTOY_HOTKEY_TIP@"
        color = "#607080"
        align = "left"
    }
}

# --- Memdisk Indicator (top right) -------------------------------------------
# Shows memdisk mode status if active
+ hbox {
    left   = 88%
    top    = 8
    width  = 10%
    height = 25

    + label {
        text  = "@VTOY_MEM_DISK@"
        color = "#e76f51"
        align = "right"
    }
}
